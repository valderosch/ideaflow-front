import React from "react";

//Navigation component
const Router = () => {
    return(
        <div className="router">
            <a href="#">Feed</a>
            <a href="#">Profile</a>
            <a href="#">Subs</a>
        </div>
    );
}

export default Router;